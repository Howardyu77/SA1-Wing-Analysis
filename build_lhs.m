function lhsmat = build_lhs(xs,ys)
%This function assemble the matrix A
np = length(xs) - 1; 
psip = zeros(np,np+1);

for j=1:1:np
    for k=1:1:np
        if j=1
            
        end 
    end
end
%initialise lhsmat
lhsmat = zeros(np+1,np+1);

end

